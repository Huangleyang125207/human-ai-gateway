# Gateway 移动端设计 Brief（给 claudedesign）

> 创建 2026-06-13 · 葱鸭 × claude-opus-4.8
> 配套读：① 桌面氛围 brief `agents/human-ai-schedule/DESIGN_BRIEF.md`（魂在那）
> ② 移动工程现状 `mobile/MOBILE_MVP_PLAN.md`（CC 已搭的骨架 + 数据层）
> 结构已对齐；视觉/动效交给你，**功能与行为一律向桌面看齐**（见下「核心原则」）。

---

## 核心原则（先读这条，它管住整份 brief）

**移动版 = 桌面版换了交互和 UI 的「同一个程序」，不是新产品。**

能为手机重做的只有两样：**① 交互逻辑（触屏手势）　② UI 呈现（布局 / 视觉 / 动效）**。

**除此之外一切向桌面看齐 —— 照搬，不改、不重新发明、不按「采用率」裁剪：**
功能集 · 数据模型（MD 是真相）· 写作协议（半小时块 / 标签 / 夹批 / authorship 边界）·
AI 行为与人格 · 业务规则（打卡窗口 / 建新天最多 +1 / 双钥匙体系）· 命名与文案口径。

> - cd 只对「长什么样、手指怎么动」负责；「是什么、怎么运作」= 桌面，照抄。
> - 第四节那张清单**不是可选项**，是"桌面有的迟早都要做，差别只是实现顺序"。
> - 任何"移动处理"写法只描述**触屏怎么呈现**，**不得改动**桌面的语义 / 数据 / 规则。
> - 拿不准某功能怎么运作 → 读桌面源码为准，别自己发明。

---

## 一、这是什么

桌面版 Gateway 已经在跑（Tauri 壳 + 半小时块日记 + 常驻 AI 对话 + 打卡 + 拖图 +
设置市集，内测 ~100 人渠道）。现在要做**手机版**，**iPhone + Android 一套壳**（Capacitor）。

**关键决定：移动版不是把桌面 UI 塞进 webview，是重做一套移动原生交互。**
我们试过克隆桌面——日期随滚动消失、加号点了没反应、卡片排不下，全是触屏死症。
所以推倒，按**传统手机 App 习惯**重做。你这次设计的就是这套移动原生界面。

**魂不变**（细节看桌面 brief，这里只钉死铁律）：安静 · 慢 · 像纸 · 私人 · 深夜低光下舒服。
MD 是真相、AI 是隐形协作者、不要 SaaS/dashboard 感。米黄纸系 + 4 色粉彩（桃/绿/雾/玫瑰）+ 宋体正文。（朱砂/夜色是另一套 paper，不用）

---

## 二、已经对齐死的移动 IA（结构定了，视觉你来）

```
┌──────────────────────────────┐
│ ☰   日记 · 对话               │ ← 顶栏第一行:汉堡最左 + 日记/对话平级切换
│      ‹ 今天·6/13 · 明天 · 灰 › │ ← 第二行:渐变滑动日期带(下详)
├──────────────────────────────┤  ↑ 整条顶栏:下滑隐藏、上滑唤回(让位内容)
│                              │
│   主内容(日记时间线 / 对话流)   │ ← 唯一滚动区
│                              │
├──────────────────────────────┤
│              ( + )           │ ← 底部:日记态=加号;对话态=输入框(下详)
└──────────────────────────────┘
```

钉死的几条：
- **顶栏**：`☰`（最左，所有"非日记非对话"的入口）+ `日记`/`对话` 平级切换。**下滑收起、上滑唤回。**
- **日期带**（渐变滑动，跟 PC 逻辑一致）：横向滑动 5 天窗口、边缘渐隐；今天高亮（金棕强调）；
  停在最新日仍露 **2 天未来灰格**；**未创建=灰**；**滑/点未来格=创建那天**，**最多只能建到明天（+1）**。
- **底部栏（你这次改的）**：
  - **日记态 = 一个「+」**。点 + → 弹出**卡片编辑器**（字段：**#tag、时间块、正文**，可含标题）。
    比纯文本输入更贴日记结构。卡片形态/弹出动画/字段排布 = 你设计。
  - **对话态 = 输入框 + 发送**（聊天就该是熟悉的底部输入，回车发、流式出字）。
- **☰ 菜单**（底部抽屉）：设置 · 聚合页 · 小组件 · 历史 · 关于（颗粒度见第四节）。

## 三、触屏交互原则（替掉桌面的鼠标习语）

| 桌面习语 | 移动改成 |
|---|---|
| hover 出工具 / 右键菜单 | **长按** 出操作菜单；或 **左滑** 出删除 |
| 小垃圾桶 icon 删除 | 条目 **左滑删除**（纸的做法，不要 icon 堆） |
| 点元素 @引用进对话 | **长按元素 → 拉进对话**（保留"指给 AI 看"语义） |
| 行内点开直接编辑 | **点条目 → 底部 sheet / 全屏卡片编辑** |
| 拖图到画布定位 | **相册选图 → 抠图 → 贴到某时间块**（touch drag 移动/缩放） |
| 顶部常驻大标题 | **顶栏下滑收起**，内容优先 |

动效方向（延桌面 brief）：呼吸、墨迹晕开、纸页翻动、磨墨等待——**安静**，不要弹跳闪烁。

---

## 四、桌面功能 → 移动：一个一个往上加（路线图）

> 这是"对照电脑版源代码往上加"的来源。每项标了**桌面出处** + **移动处理(仅触屏呈现)** + **批次**。
> 批次只是**实现顺序,不是要不要**：🟢 已做 · 🟡 下一批 · ⚪ 后续批次。**全部在范围内**;
> "移动处理"列只说触屏怎么点/滑/弹,**功能行为、数据、文案一律照桌面,不改语义**。

### A. 单日日记（核心面）
| 功能 | 桌面出处 | 移动处理 | 阶段 |
|---|---|---|---|
| 半小时块时间线 | journal.js / parse_journal | 时间在左、tag+标题+正文在右,竖向流 | 🟢 |
| 新建条目 | insert-block | **+ → 卡片编辑器(#tag/时间/正文)** | 🟡 |
| 行内编辑正文 | patch-block | 点条目 → 卡片/sheet 编辑 | 🟡 |
| 删除块 | delete-block | 左滑删除 | 🟡 |
| 空白天 / 创建今天 | new-day | 日期带滑到今天自动落骨架 | 🟢 |
| 夹批 / #commit 双签 | journal.js commit | 条目内次级展开(低频,可后置) | ⚪ |
| authorship(@user 不可覆盖) | 后端 8 层守卫 | 移动遵守即可,无需 UI | — |

### B. 打卡 daily-tasks
| 功能 | 桌面出处 | 移动处理 | 阶段 |
|---|---|---|---|
| 补剂/自定义打卡 | ritual.js | 横滑卡片,点击 toggle | 🟢 |
| 八杯水(多剂量) | water-cup / ritual | 点一次加一杯,长按清 | 🟡 |
| 打卡图标(拖图设标) | scrapbook+task | 卡片长按 → 换图标 | ⚪ |
| 打卡管理(加/删/剂量) | daily-tasks/meta | ☰小组件 → 管理页 | 🟡 |
| 补昨天 backfill | _writable_dates | 日期带回看昨天可补(次日12点前) | ⚪ |

### C. 对话流 thread
| 功能 | 桌面出处 | 移动处理 | 阶段 |
|---|---|---|---|
| 常驻 AI 对话(流式) | thread.js | 对话 tab,底部输入,流式气泡 | 🟢骨架/🟡真连 |
| 21:30 纸条仪式 | notes-board.js | 对话流内的特别纸条(慢场景) | 🟡 |
| 留言板视图 | notes-board | 视图切换(对话 ↔ 留言板) | ⚪ |
| 两种笔迹(人/AI) | classic `--serif` / `--hand` | 已就位 | 🟢 |
| past_boards 跨夜注入 | 后端 | 移动继承,无需 UI | — |
| 压缩圆环(记忆快照) | compact-ring.js | 对话顶/旁小指示,点压缩 | ⚪ |

### D. 拖图 scrapbook(手账贴纸)
| 功能 | 桌面出处 | 移动处理 | 阶段 |
|---|---|---|---|
| 贴图到时间块(浮动) | scrapbook.js | 相册选图 → 浮在对应块 | ⚪ |
| 端侧抠图 | cutout / vision | 选图后自动抠(转圈等待态) | ⚪ |
| AI 自动定位(vision) | vision-router | 后端,要阿里云钥匙 | ⚪ |

### E. 设置 / onboarding（☰ → 设置）
| 功能 | 桌面出处 | 移动处理 | 阶段 |
|---|---|---|---|
| 双钥匙 onboarding | setup.js / consent.html | 首启引导:DeepSeek=声音 / 阿里云=眼睛 | 🟡 |
| 填/测 钥匙 | setup save/test | 设置 → 钥匙页,粘贴+测试 | 🟡 |
| 模型选择 | models | 设置 → 模型 | ⚪ |
| 皮肤 / 日夜 | theme.js | classic 单一日间;日/夜随另一套 paper 上线再说 | ⚪ |
| 数据 / consent / 撤回 | consent / telemetry / forget | 设置 → 数据 | ⚪ |
| 诊断 | silent-failures | 设置 → 诊断(低频) | ⚪ |
| 关于 | — | 设置 → 关于 | ⚪ |
| 插件市集(装/卸 widget) | marketplace.js | ☰小组件 | ⚪ |

### F. 聚合 / 历史 / 总览
| 功能 | 桌面出处 | 移动处理 | 阶段 |
|---|---|---|---|
| 标签聚合页 | aggregate.js / tag-aggregate | ☰聚合页:按 tag 横切看 | ⚪ |
| 项目 PULSE 脉搏 | pulse 路由 | ☰聚合页内 or 单独脉搏卡 | ⚪ |
| 往日翻阅 | history.html | 日期带翻 + ☰历史长列表 | 🟡 |
| widget 总览拼装 | widget-loader.js | 移动暂收进日记页顶部/小组件,不做拼装画布 | ⚪ |

### G. 系统(多数移动端无需重画)
自动更新→App Store/TestFlight 接管 · Korok 彩蛋⚪ · vault audit/silent-failure→后端继承 · 启动屏🟡。

---

## 五、硬约束（违反 = 稿子落不了地，工程红线）

1. **vanilla,无 build/npm/框架**（web 层）。数据全走本地 `/api/*`(CC 已写的 shim 拦截层
   读写手机本地 MD + 直连 DeepSeek)。你设计别假设别的数据管线。
2. **markdown 渲染单一入口 `window.gatewayMd()`**（marked+DOMPurify),正文别另造渲染。
3. **classic 调色板已存在**(`style.css` `:root`:米黄 #f4ede2 + 4 粉彩 + 墨阶)——复用,别引新色。
   **classic 单一日间、无夜色**(日/夜 oklch 那套是另一套 paper 的事,不在本轮)。
4. **字体走系统 CJK 衬线**(iOS Songti/Kaiti、Android 思源),**零 CDN**(离线/代理下会白屏)。
5. **触屏优先**:点击热区 ≥44pt;无 hover 依赖;手势(滑/长按)要有视觉提示。
6. **safe-area**:刘海/底部 home 条要让位(env(safe-area-inset-*));顶栏/底栏别被吃。
7. **动画安静**:呼吸/墨迹/纸页,不要 dashboard 闪跳;尊重 reduced-motion。

## 全色板（私印小报 / 第一套，照 `shared/style.css` `:root`，cd 直接用、别另取色）

> 移动端做的是**第一套 classic（私印小报）**：**米黄底 + 4 色粉彩 + 金棕链接**，纯 hex/rgb、**单一日间**。
> ⚠ 朱砂 `#b14a2b`、oklch 夜色、`--paper-*` 那套是**另一套「纸与灯」(paper) 皮肤**，**不在本轮范围，别引**。

| 变量 | 角色 | 值 |
|---|---|---|
| `--bg` | 主背景（米黄） | `#f4ede2` |
| `--bg-2` | 次级面 | `#efe6d6` |
| `--bg-3` | 三级面 / 卡片底 | `#e8dec9` |
| `--ink` | 正文墨 | `#3a342c` |
| `--ink-2` | 次级文字 | `#5a4f42` |
| `--ink-3` | 弱文字 | `#857a68` |
| `--ink-4` | 微弱 / 时间 / 标签 | `#a89c84` |
| `--peach` | 粉彩·桃 | `#e9c7b4` |
| `--sage` | 粉彩·绿 | `#cdd5b9` |
| `--mist` | 粉彩·雾蓝 | `#c8d0d8` |
| `--rose` | 粉彩·玫瑰 | `#ddb7b1` |
| `--line` | 细分隔线 | `rgb(58 52 44 / 0.08)` |
| `--line-2` | 强分隔线 | `rgb(58 52 44 / 0.14)` |
| `--warm` | 暖洗 | `rgb(233 199 180 / 0.22)` |
| `--warm-2` | 暖洗强 / 选区 | `rgb(233 199 180 / 0.42)` |

**散在组件、未进 `:root` 的语义色（style.css 硬编码；cd 照用，CC 落地补进 `:root`）：**
- 链接 / 角色标签：`#b4731e`（金棕），hover `#8a5712`（深金棕）
- @引用 / pending 标记：`#b87a4f`（浅棕 tan）
- 极少用：纯白 `#fff` / 纯黑 `#000`

**字体**：人 / 正文 `--serif`(Noto Serif SC / Songti)、UI `--sans`(Inter)、手写 `--hand`(Caveat / 思源宋)。

> 铁律：只用上表（第一套 classic）的色。**绝不引「纸与灯」那套**（朱砂 / oklch 夜色 / `--paper-*`）。
> classic **单一日间、无夜色**——夜间模式是 paper 的事，这轮不做。新色先进 `:root` 变量，别散落硬编码。

---

## 六、要你（cd）出什么

参照桌面那轮"cd 出素材、CC 组装"的分工:

1. **关键屏静态稿**(竖屏,定气味+密度+呼吸):
   - 日记页(顶栏+日期带+打卡横滑+时间线)
   - **+ 卡片编辑器**(#tag/时间/正文,弹出形态)← 本次重点
   - 对话页(气泡/纸条/底部输入)
   - ☰ 菜单抽屉 + 设置页(钥匙/皮肤日夜)
2. **触屏交互图记/动效**:日期带滑动+创建明天、条目左滑删、长按拉进对话、+ 卡片弹出、磨墨等待。
3. **移动组件 CSS reference**(基于现有 token,补移动专属:顶栏/日期带/打卡卡/底部+/卡片编辑器/抽屉/sheet)。

**不用出整页可用 mock**(组装归 CC):接 gatewayMd/真数据/手势实现/safe-area/性能在 CC 侧。

**边界再钉一次**:cd 只产出**样子 + 手势**;**功能集/行为/数据/写作协议/AI 人格/文案**
不用你操心也**不要你定**——那些 CC 对着桌面源码 1:1 落地。你拿不准某功能怎么运作,
照桌面现状画即可(或问 CC 调桌面行为),**别为移动端重新设计它的逻辑**。

## 七、现状（CC 已搭的骨架,你在这之上设计）

`mobile/m/`(index+css+js)已跑通并在 iPhone 模拟器验证:☰·日记·对话顶栏(下滑收起)+
渐变日期带(今天/明天可建/+2 灰,最多+1)+ 打卡横滑 + 时间线 + 底部输入(待按本 brief 改成 +)。
复用 shim 数据层 + gatewayMd。**iOS 构建管线(Capacitor→Xcode)已通。**
（注:骨架现暂误链了 paper 的 `design-tokens.css`+用了朱砂/--paper-*,**待换成 classic `style.css` 的米黄+4粉彩调色板**——恢复建时第一件事。）
你出稿后 CC 据稿替换/补齐视觉与手势,功能按第四节路线图一个一个往上加。

> 一句话给你:把桌面那本"安静的纸杂志"，翻译成单手能用、触屏顺手的口袋版——
> 熟悉的手机交互骨架里，装的还是那张纸、那盏深夜的灯。
