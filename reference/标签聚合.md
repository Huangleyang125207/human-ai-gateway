# 标签聚合

> 项目维度的横切索引。带项目 tag 的 H2 段,**同一轮**镜像一行进来。generic tag 不进。

vault 还没有项目 tag 进聚合。等你跟 AI 在用的过程里自然形成反复出现的项目方向,AI 会问一句「`#X` 要不要收进聚合页?」,同意了 AI 会在下面开一个 `## #X` section 收行。

每行格式:

```
- [文件链接](半小时复盘/YY.M.D(第N天).md#H1:MM) — 一句话概要
```

同一 section 内按时间倒序,新行 prepend(最近的在最上面)。
